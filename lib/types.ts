export type {
  User,
  Couple,
  Episode,
  ActualResult,
  Prediction,
  Account,
  Session,
} from "@prisma/client";

export { Role, CoupleStatus, EpisodeStatus, PredictionKind } from "@prisma/client";
